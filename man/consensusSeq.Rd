% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/consensus.R
\name{consensusSeq}
\alias{consensusSeq}
\title{Compute the consensus sequence of a multiple sequence alignment}
\usage{
consensusSeq(aln, gap.chars = "-", include.gaps = TRUE)
}
\arguments{
\item{aln}{Alignment, accepted in the same forms as \code{\link[=msa2DF]{msa2DF()}}.}

\item{gap.chars}{Character vector of one or more single-character strings
treated as gaps.}

\item{include.gaps}{Logical. If \code{TRUE} (default), gap characters count
toward the per-position consensus. If \code{FALSE}, gaps are dropped from
the count and the most common non-gap letter wins; positions that are
entirely gaps fall back to \code{gap.chars[1]}.}
}
\value{
A single character string of length equal to the alignment width.
}
\description{
Returns the most common character at each position. By default, gap
characters compete with letters when picking the most common; set
\code{include.gaps = FALSE} to ignore gaps when the alphabet supports a clear
winner at a position.
}
\details{
Ties between equally common characters are broken by occurrence order in
the input (the first letter to appear wins).
}
\examples{
aln <- c(
  seq1 = "ACGTACGT",
  seq2 = "ACGTTCGT",
  seq3 = "ACG-ACGT"
)
consensusSeq(aln)
consensusSeq(aln, include.gaps = FALSE)

}
