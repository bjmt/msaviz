% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/msa2df.R
\name{msa2DF}
\alias{msa2DF}
\title{Convert a multiple sequence alignment to a tidy data frame}
\usage{
msa2DF(aln, reference = NULL, drop.gaps = TRUE, gap.chars = "-",
  keep.consensus = FALSE, consensus.gaps = TRUE, uppercase = FALSE,
  verbose = FALSE)
}
\arguments{
\item{aln}{Alignment to convert. See Details.}

\item{reference}{Name of the sequence in \code{aln} to use as reference. When
\code{NULL}, a consensus sequence is computed and used.}

\item{drop.gaps}{Logical. If \code{TRUE} (default), rows where the reference or
the sequence holds a gap character are dropped from the returned tibble.
If \code{FALSE} gaps are kept and marked with \code{Aln = "Gap"} and
\code{Letter = NA}.}

\item{gap.chars}{Character vector of one or more single-character strings
treated as gaps.}

\item{keep.consensus}{Logical. When \code{reference} is \code{NULL}, controls whether
the computed consensus row is kept in the output. Defaults to \code{FALSE}.}

\item{consensus.gaps}{Logical. When computing the consensus, count gaps
when picking the most-common character (\code{TRUE}) or ignore them
(\code{FALSE}).}

\item{uppercase}{Logical. If \code{TRUE}, force every letter to upper case
before comparing to the reference. Useful when input sequences mix cases.}

\item{verbose}{Logical. Print progress and size diagnostics to the console.}
}
\value{
A \link[tibble:tibble-package]{tibble} with columns \code{Sequence}
(factor), \code{Position} (integer), \code{Letter} (factor), and \code{Aln} (factor with
levels \code{Ref}, \code{Alt}, \code{Gap}). The result carries the attributes
\code{aln.size}, \code{drop.gaps} and \code{reference}.
}
\description{
Reshapes an aligned set of equal-length sequences into a long-format tibble
with one row per (sequence, position), suitable for passing to
\code{\link[=msaHeatmap]{msaHeatmap()}} or any other ggplot2-based visualization.
}
\details{
\code{aln} can be a named character vector (one element per sequence), an
\code{alignment} object as returned by \code{\link[seqinr:read.alignment]{seqinr::read.alignment()}}, or any
object that yields a character vector through \code{\link[base:character]{base::as.character()}}
(e.g. a \code{Biostrings} \code{DNAMultipleAlignment} or \code{AAMultipleAlignment}).
When \code{reference} is \code{NULL} (the default) a consensus sequence is computed
and used as the reference for the \code{Aln} column.

Sequence names must be unique; duplicates are rejected with an error.
}
\examples{
aln <- c(
  seq1 = "ACGTACGT",
  seq2 = "ACGTTCGT",
  seq3 = "ACG-ACGT"
)
msa2DF(aln, reference = "seq1")

}
