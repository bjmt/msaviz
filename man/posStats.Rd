% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posStats.R
\name{posStats}
\alias{posStats}
\title{Per-position summary statistics for an alignment}
\usage{
posStats(alnDF, include.gaps = FALSE)
}
\arguments{
\item{alnDF}{A tibble produced by \code{\link[=msa2DF]{msa2DF()}} containing the columns
\code{Sequence}, \code{Position}, \code{Letter}, and \code{Aln}.}

\item{include.gaps}{Logical. If \code{TRUE}, gap characters are included when
computing \code{mostCommon} and \code{entropy}. If \code{FALSE} (default) gaps are
excluded from those statistics.}
}
\value{
A tibble with one row per position and columns \code{Position}, \code{n}
(non-gap count), \code{nGaps}, \code{pctIdentity} (\code{n_Ref / n}), \code{mostCommon},
\code{mostCommonPct}, and \code{entropy} (Shannon, natural log).
}
\description{
Computes a small set of per-position summaries from a tidy alignment data
frame as produced by \code{\link[=msa2DF]{msa2DF()}}: how many non-gap entries cover the
position, how many gaps, the fraction matching the reference (\code{Aln == "Ref"}), the most common letter, and the Shannon entropy of the column.
}
\details{
The result has one row per alignment position, including positions that
are entirely gaps (which receive \code{NA} for letter-based stats).
}
\examples{
aln <- c(
  seq1 = "ACGTACGT",
  seq2 = "ACGTTCGT",
  seq3 = "ACGAACGT",
  seq4 = "ACGTACGT"
)
posStats(msa2DF(aln, reference = "seq1"))

}
