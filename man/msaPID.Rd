% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/msaPID.R
\name{msaPID}
\alias{msaPID}
\title{Per-position percent identity track}
\usage{
msaPID(alnDF, aln.size = NULL, style = c("bar", "line"), fill = "grey40",
  line.colour = "black", text.size = 6, hide.x.labels = TRUE)
}
\arguments{
\item{alnDF}{A tibble produced by \code{\link[=msa2DF]{msa2DF()}}.}

\item{aln.size}{Total alignment width in positions. If \code{NULL} (default)
it is taken from \code{attr(alnDF, "aln.size")} and falls back to
\code{max(alnDF$Position)}.}

\item{style}{One of \code{"bar"} (default, \code{geom_col}) or \code{"line"}
(\code{geom_step}).}

\item{fill, line.colour}{Aesthetics for the bar fill or step line.}

\item{text.size}{Size in pt of axis text.}

\item{hide.x.labels}{Hide the x-axis text + ticks. Default \code{TRUE} so
the track plays nicely as a top strip over a heatmap.}
}
\value{
A ggplot object.
}
\description{
Builds a ggplot showing the fraction of non-gap cells at each alignment
position that match the reference sequence (\code{Aln == "Ref"}). The x-axis
is configured to line up exactly with \code{\link[=msaHeatmap]{msaHeatmap()}} so the two plots
can be stacked via \code{\link[=composeMSA]{composeMSA()}} (or any patchwork composition) with
shared coordinates.
}
\examples{
aln <- c(
  seq1 = "ACGTACGT",
  seq2 = "ACGTTCGT",
  seq3 = "ACGAACGT",
  seq4 = "ACGTACGT"
)
msaPID(msa2DF(aln, reference = "seq1"))

}
