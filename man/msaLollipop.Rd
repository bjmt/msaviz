% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/msaLollipop.R
\name{msaLollipop}
\alias{msaLollipop}
\title{Per-sequence lollipop chart of alignment differences}
\usage{
msaLollipop(alnDF, highlight = "Alt", highlight.by = "Aln", y = NULL,
  labels = TRUE, baseline = TRUE, drop.empty = TRUE,
  row.order = levels(alnDF$Sequence), names.pos = c("right", "left"),
  trim.names = TRUE, trim.names.nchar = 25, stem.height = 0.7,
  point.size = 2, point.shape = 21, point.fill = "white",
  point.colour = "black", head.fill.by = NULL, head.fill.colours = NULL,
  segment.lwd = 0.5, segment.colour = "black", baseline.lwd = 0.5,
  baseline.colour = "black", label.size = 2, label.nudge.y = 0.25,
  text.size = 6, hide.x.labels = TRUE, x.breaks = NULL,
  x.labels = NULL)
}
\arguments{
\item{alnDF}{A tibble produced by \code{\link[=msa2DF]{msa2DF()}}. Must contain \code{Sequence},
\code{Position}, \code{Letter}, and \code{Aln}.}

\item{highlight}{Character vector of values in \code{highlight.by} to draw
lollipops for. Defaults to \code{"Alt"} (every non-reference cell).}

\item{highlight.by}{Column of \code{alnDF} to match \code{highlight} against.
Defaults to \code{"Aln"}, so the natural "lollipop every SNP" case works
regardless of \verb{column =} choices upstream. Set to \code{"Letter"} to
lollipop specific residues.}

\item{y}{Optional column name in \code{alnDF} giving a numeric stem height
per cell. When \code{NULL} (default), every highlighted cell gets a stem
of height 1. When supplied, heights are normalised against
\code{max(alnDF[[y]], na.rm = TRUE)} so the tallest stem still fits
within \code{stem.height}.}

\item{labels}{Logical. Draw \code{RefPosAlt} labels above each head. When
the reference sequence is not present in \code{alnDF} (e.g. the
\code{msa2DF(..., keep.consensus = FALSE)} default consensus path),
labels fall back to position-only (\code{"123T"}) and a one-time message
points at \code{keep.consensus = TRUE}.}

\item{baseline}{Logical. Draw per-position horizontal baseline
segments. Gaps in the alignment show as visible breaks.}

\item{drop.empty}{Logical. Drop sequences with no highlighted cells
before plotting.}

\item{row.order}{Character vector giving the sequence display order,
top to bottom. Names not present in \code{alnDF$Sequence} are kept as
empty rows, which reproduces the "spacer" trick from the paper.}

\item{names.pos}{Side of the plot where sequence names are drawn.}

\item{trim.names}{Logical. Trim long sequence names with an ellipsis.}

\item{trim.names.nchar}{Max number of characters in a trimmed name.}

\item{stem.height}{Maximum stem height within a row, in row-height
units. Default \code{0.7} leaves 30\% of each row free for the label.}

\item{point.size, point.shape, point.fill, point.colour}{Aesthetics for
the lollipop heads. \code{point.fill} is the constant fill colour used
when \code{head.fill.by} is \code{NULL} (the default).}

\item{head.fill.by}{Optional column name in \code{alnDF}. When set, each
head is filled according to that column's value, useful for
colouring SNPs by residue identity (\code{head.fill.by = "Letter"}) or
by SNP category (\code{head.fill.by = "Aln"}). Defaults to \code{NULL}
(constant \code{point.fill}).}

\item{head.fill.colours}{Named character vector of fill colours,
used with \code{head.fill.by}. When \code{NULL}, ggplot's default discrete
palette is used. The bundled palettes \link{msa_palette_DNA},
\link{msa_palette_RNA}, and \link{msa_palette_AA} are well-suited here.}

\item{segment.lwd, segment.colour}{Aesthetics for the stems.}

\item{baseline.lwd, baseline.colour}{Aesthetics for the baseline.}

\item{label.size}{Size of the \code{geom_text} labels above the heads.}

\item{label.nudge.y}{Vertical offset of labels above each head, in
units of \code{stem.height} (so labels track the configured stem
height).}

\item{text.size}{Size of the sequence-name axis text.}

\item{hide.x.labels}{Hide the x-axis text and ticks. Default \code{TRUE} so
the plot reads clean as in the paper.}

\item{x.breaks, x.labels}{Override the default x-axis breaks and
labels.}
}
\value{
A ggplot object. Carries \code{row.order}, \code{legend.pos},
\code{legend.nrow}, and \code{aln.size} attributes so that \code{\link[=composeMSA]{composeMSA()}} can
stack it with companion plots the same way it stacks \code{\link[=msaHeatmap]{msaHeatmap()}}
output.
}
\description{
Builds a ggplot lollipop chart from a long-format alignment data frame
(as produced by \code{\link[=msa2DF]{msa2DF()}}). One row of the discrete y-axis is allocated
per sequence, and each highlighted cell within a row gets a vertical
stem topped with a circular head and an optional \verb{RefLetter Position AltLetter} label (e.g. \code{A123T}). A per-position horizontal baseline
runs along each row at \code{y = 0} so that gaps in the alignment show up
as visible breaks in the baseline.
}
\details{
This is the function that drew Supplementary Figure 20 of Hodgins et
al. (2023), \emph{Nature Communications} 14:5475, generalised so it can be
used as a drop-in substitute for \code{\link[=msaHeatmap]{msaHeatmap()}} inside \code{\link[=composeMSA]{composeMSA()}}
(dendrogram, row + column group strips, and a PID track all line up
with lollipop rows).
}
\examples{
aln <- c(
  seq1 = "ACGTACGT",
  seq2 = "ACGTTCGT",
  seq3 = "ACGAACGT"
)
alnDF <- msa2DF(aln, reference = "seq1", drop.gaps = FALSE,
  keep.consensus = TRUE)
msaLollipop(alnDF)

}
