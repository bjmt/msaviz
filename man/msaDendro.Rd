% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/msaDendro.R
\name{msaDendro}
\alias{msaDendro}
\title{Hierarchical-clustering dendrogram of an alignment}
\usage{
msaDendro(aln, dist.matrix = c("identity", "similarity"),
  hclust.method = "average", line.colour = "black", flip = TRUE)
}
\arguments{
\item{aln}{Alignment, accepted in the same forms as \code{\link[=msa2DF]{msa2DF()}}.}

\item{dist.matrix}{Passed through as \verb{matrix =} to
\code{\link[seqinr:dist.alignment]{seqinr::dist.alignment()}}. One of \code{"identity"} (default) or
\code{"similarity"}.}

\item{hclust.method}{Linkage method passed to \code{\link[stats:hclust]{stats::hclust()}}.
Defaults to \code{"average"}.}

\item{line.colour}{Colour of dendrogram segments.}

\item{flip}{Logical. If \code{TRUE} (default) the dendrogram is rotated so
the root is on the left and the leaves are on the right, ready to
sit alongside a heatmap. If \code{FALSE}, the conventional bottom-up
layout is used.}
}
\value{
A ggplot object with an \code{"order"} attribute giving the leaf
order as a character vector of sequence names.
}
\description{
Computes a pairwise distance matrix between sequences with
\code{\link[seqinr:dist.alignment]{seqinr::dist.alignment()}}, runs \code{\link[stats:hclust]{stats::hclust()}} on it, and renders the
resulting dendrogram as a ggplot via \code{\link[ggdendro:dendro_data]{ggdendro::dendro_data()}}. Returns a
ggplot whose y-coordinates line up with \code{\link[=msaHeatmap]{msaHeatmap()}} when the heatmap
uses the same row order, so it can be placed to the left of a heatmap
via \code{\link[=composeMSA]{composeMSA()}} (or any patchwork composition).
}
\details{
The clustered order of sequences is attached to the returned plot as the
\code{"order"} attribute, ready to pass to \code{msaHeatmap(row.order = ...)}.

Requires the \code{seqinr} and \code{ggdendro} packages (declared in \code{Suggests}).
}
\examples{
\dontrun{
aln_path <- system.file("extdata", "hug_ribosomal.fa.gz", package = "msaviz")
aln <- seqinr::read.alignment(aln_path, format = "fasta")
d <- msaDendro(aln)
head(attr(d, "order"))
}

}
