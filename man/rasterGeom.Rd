% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{rasterGeom}
\alias{rasterGeom}
\title{Rasterize a ggplot geom layer}
\usage{
rasterGeom(geomObj, png.dpi = 150, png.bg = NA,
  png.type = getOption("bitmapType"), png.antialias = "none",
  raster.interpolate = FALSE)
}
\arguments{
\item{geomObj}{A geom layer to rasterize, e.g. the return value of
\code{\link[ggplot2:geom_tile]{ggplot2::geom_tile()}}.}

\item{png.dpi}{Resolution in dots per inch for the rasterized layer.}

\item{png.bg}{Background colour painted under the rasterized geom. Defaults
to \code{NA} (transparent).}

\item{png.type}{Graphics device type passed through to \code{\link[grDevices:png]{grDevices::png()}}.
Defaults to \code{getOption("bitmapType")}. When that is \code{NULL} (e.g. on a
headless Linux server), \code{\link[grDevices:png]{grDevices::png()}}'s own built-in default is
used.}

\item{png.antialias}{Antialiasing mode passed through to \code{\link[grDevices:png]{grDevices::png()}}.}

\item{raster.interpolate}{Logical. Bilinear interpolation when the raster
is drawn back onto the panel.}
}
\value{
A ggproto layer object that can be added to a ggplot the same way
the original \code{geomObj} would be.
}
\description{
Wraps a ggplot2 geom so that, at draw time, the geom is rendered into an
in-memory PNG via \code{\link[grDevices:png]{grDevices::png()}} and the result is laid back onto the
plot as a \code{\link[grid:grid.raster]{grid::rasterGrob()}}. This produces compact, fast-rendering
output for plots with many tiles or rectangles (e.g. large
\code{\link[=msaHeatmap]{msaHeatmap()}}s), at the cost of resolution. Antialiasing and bilinear
interpolation are both off by default, giving crisp blocky output well
suited to alignment heatmaps; turn them on if you need smooth gradients.
}
\examples{
\dontrun{
library(ggplot2)
ggplot(mtcars, aes(wt, mpg)) + rasterGeom(geom_point())
}

}
