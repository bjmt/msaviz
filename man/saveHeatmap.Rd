% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/msaHeatmap.R
\name{saveHeatmap}
\alias{saveHeatmap}
\title{Save a multiple sequence alignment heatmap}
\usage{
saveHeatmap(msaHeatmap, file, height = NULL, width = NULL, unit = "mm",
  ...)
}
\arguments{
\item{msaHeatmap}{A ggplot object as produced by \code{\link[=msaHeatmap]{msaHeatmap()}}.}

\item{file}{Output file path. Format is inferred from the extension by
\code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}

\item{height, width}{Numeric image dimensions. When \code{NULL}, computed from
the number of sequences and the axis text size.}

\item{unit}{Length unit used for \code{height} and \code{width}. Defaults to \code{"mm"}.}

\item{...}{Additional arguments forwarded to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}
}
\value{
Invisibly returns the path of the saved file.
}
\description{
Thin wrapper around \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} that auto-derives a sensible height
(scaling with the number of sequences) and width (at least twice the height
or 100 mm, whichever is larger) when none are supplied. Pass \code{height} and
\code{width} explicitly to override.
}
\examples{
\dontrun{
aln <- c(seq1 = "ACGTACGT", seq2 = "ACGTTCGT", seq3 = "ACGAACGT")
p <- msaHeatmap(msa2DF(aln, reference = "seq1"))
saveHeatmap(p, tempfile(fileext = ".pdf"))
}

}
