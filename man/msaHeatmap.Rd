% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/msaHeatmap.R
\name{msaHeatmap}
\alias{msaHeatmap}
\title{Draw a multiple sequence alignment as a heatmap}
\usage{
msaHeatmap(alnDF, column = c("Aln", "Letter"), gap.colour = NA,
  aln.colours = c(Alt = "red", Ref = "black"), letter.colours = NULL,
  row.order = levels(alnDF$Sequence), names.pos = c("right", "left"),
  legend.pos = c("top", "bottom", "left", "right", "none"),
  legend.nrow = 1, raster = TRUE, raster.dpi = 150,
  raster.type = getOption("bitmapType"), text.size = 6,
  text.size.x = text.size, text.size.y = text.size,
  text.size.legend = text.size, trim.names = TRUE, trim.names.nchar = 25,
  x.breaks = NULL, x.labels = NULL, y.breaks = NULL, y.labels = NULL,
  hide.x.labels = FALSE, hide.y.labels = FALSE, x.labels.rotate = 0,
  y.labels.rotate = 0, emphasize = "Alt", emphasize.by = "Aln",
  emphasize.size = 1, emphasize.size.x = emphasize.size,
  emphasize.size.y = emphasize.size)
}
\arguments{
\item{alnDF}{A tibble produced by \code{\link[=msa2DF]{msa2DF()}}. Must contain at minimum the
\code{Sequence} and \code{Position} columns plus whichever of \code{Aln} / \code{Letter} is
chosen by \code{column}.}

\item{column}{Which column of \code{alnDF} to map to the fill aesthetic. One of
\code{"Aln"} (default) or \code{"Letter"}.}

\item{gap.colour}{Fill colour for \code{NA} (gap) tiles. Defaults to \code{NA},
leaving them transparent.}

\item{aln.colours}{Named character vector of fill colours, used when
\code{column = "Aln"}. Defaults to \code{c(Alt = "red", Ref = "black")}.}

\item{letter.colours}{Named character vector of fill colours, used when
\code{column = "Letter"}. When \code{NULL} ggplot's default discrete palette is
used.}

\item{row.order}{Character vector giving the sequence display order, top to
bottom. Defaults to the factor levels in \code{alnDF$Sequence}.}

\item{names.pos}{Side of the plot where sequence names are drawn.}

\item{legend.pos}{Where to place the fill legend, or \code{"none"} to suppress
it.}

\item{legend.nrow}{Number of rows in the legend.}

\item{raster}{Logical. Rasterize the tile layer via \code{\link[=rasterGeom]{rasterGeom()}} to
shrink vector output and speed up rendering. Defaults to \code{TRUE}.}

\item{raster.dpi}{DPI for the rasterized layer.}

\item{raster.type}{Graphics device type passed through to \code{\link[grDevices:png]{grDevices::png()}}.}

\item{text.size, text.size.x, text.size.y, text.size.legend}{Sizes (in pt) for
the various text elements. The three specific ones default to the value
of \code{text.size}.}

\item{trim.names}{Logical. Trim long sequence names with an ellipsis.}

\item{trim.names.nchar}{Max number of characters in a trimmed name.}

\item{x.breaks, x.labels, y.breaks, y.labels}{Override the default x/y scale
breaks and labels.}

\item{hide.x.labels, hide.y.labels}{Hide axis text and ticks on the
respective axis.}

\item{x.labels.rotate, y.labels.rotate}{Rotation angle in degrees applied
to the axis text on each axis. Defaults to \code{0} (no rotation). Useful
for fitting every position label on long alignments
(\code{x.labels.rotate = 90} plus a denser \verb{x.breaks =}).}

\item{emphasize}{Character vector of values to emphasize by drawing the
matching cells as enlarged tiles overlaid on the base layer. Defaults
to \code{"Alt"} (the natural "make SNPs pop" case). Set to \code{NULL} or
\code{character(0)} to disable. The overlay only renders when at least one
of \code{emphasize.size.x} or \code{emphasize.size.y} is greater than \code{1}.}

\item{emphasize.by}{Name of the column in \code{alnDF} to match \code{emphasize}
against. Defaults to \code{"Aln"} so the SNP case works regardless of
\code{column}. Set to \code{"Letter"} to enlarge specific residues.}

\item{emphasize.size}{Master expansion factor for emphasized cells.
Defaults to \code{1} (no overlay).}

\item{emphasize.size.x, emphasize.size.y}{Per-axis expansion factors.
Default to \code{emphasize.size}. Values greater than \code{1} make emphasized
tiles overlap their neighbours on the corresponding axis. Cells at
the edge of the alignment (position \code{1}, position \code{aln.size}, first
or last sequence row) are clipped to the panel so they never extend
past the plot limits.}
}
\value{
A ggplot object.
}
\description{
Builds a ggplot heatmap from a long-format alignment data frame (as
produced by \code{\link[=msa2DF]{msa2DF()}}). Each tile is one (sequence, position) cell. Colour
can encode the per-letter identity of each cell (\code{column = "Letter"}) or
the relationship to a reference sequence (\code{column = "Aln"}, default).
}
\details{
For large alignments, \code{raster = TRUE} (the default) rasterizes the tile
layer at the supplied DPI, producing a much smaller and faster output than
a vector layer of millions of rectangles.
}
\examples{
aln <- c(
  seq1 = "ACGTACGT",
  seq2 = "ACGTTCGT",
  seq3 = "ACGAACGT"
)
alnDF <- msa2DF(aln, reference = "seq1")
msaHeatmap(alnDF)

}
